# Keeps the GitHub Actions used in CI up to date (opens a small PR when a new
# version is out; CI runs on it like any other change).
version: 2
updates:
  - package-ecosystem: github-actions
    directory: /
    schedule:
      interval: monthly
    groups:
      actions:
        patterns: ["*"]
